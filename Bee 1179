pares = [0] * 5
impares = [0] * 5
n1, n2 = 0, 0
for i in range (15):
    N = int(input( ))
    if N%2 == 0:
        pares[n1] = N
        n1 += 1
        if n1 == 5:
            for i in range(5):
                print(f"par[{i}] = {pares[i]}")
            n1 = 0
    else:
        impares[n2] = N
        n2 += 1
        if n2 == 5:
            for i in range(5):
                print(f"impar[{i}] = {impares[i]}")
            n2 = 0

i = 0
while i != n2:
    print(f"impar[{i}] = {impares[i]}")
    i += 1
i = 0
while i != n1:
    print(f"par[{i}] = {pares[i]}")
    i += 1
